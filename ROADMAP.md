# Дорожная карта NoteInSite

Составлена 18.09.2026 по итогам разбора кода (`src/`, ~2700 строк), сайта и процесса выпуска.
Порядок этапов — по убыванию отдачи, а не по сложности. Внутри этапа пункты можно брать
в любом порядке, если не указана зависимость.

Обозначения трудоёмкости: **S** — до часа, **M** — половина дня, **L** — день и больше.

---

## Этап 0. Фундамент — сделано

| Пункт | Статус | Объём |
| --- | --- | --- |
| `git init`, `.gitignore`, `.gitattributes`, первый коммит | готово | S |
| Репозиторий на GitHub, тег `v1.1.0` | готово | S |

**Зачем.** Сейчас 2700 строк рабочего кода, сайт и вся история решений живут в одной папке
без истории: неудачную правку не откатить, версию расширения не сравнить с предыдущей,
`dist/*.zip` служат вместо тегов. Всё остальное в этой карте безопасно делать только после
этого пункта.

**Отдельно:** `data.txt` с паролем от FTP лежит прямо в папке проекта. Он внесён
в `.gitignore`, но правильнее убрать его из проекта совсем — туда же, где лежат реквизиты
других проектов.

---

## Этап 1. Сохранность данных

Самое неприятное, что может случиться с приложением для заметок, — молча потерять заметку.
Сейчас есть три способа это сделать.

### 1.1. Переполнение хранилища не видно — **сделано**

Было: `chrome.storage.local` без `unlimitedStorage` ограничен 10 МБ, а `saveAll()` глушил
любую ошибку записи пустым `catch` — заметка не сохранялась молча.

Сделано:

* `unlimitedStorage` в манифесте;
* неудачная запись показывается красным сообщением, которое не гаснет само, и повторяется
  через 1,5, 5 и 15 секунд; отдельно разобраны переполнение и перезапуск расширения
  (повторять нечем — нужна перезагрузка страницы, это видно по `chrome.runtime.id`);
* удачная запись после сбоя снимает сообщение и подтверждает: «Заметки сохранены»;
* размер хранилища показан в окне расширения, после 8 МБ — напоминание про экспорт;
* сверх плана: по `pagehide` и уходу вкладки в фон несохранённое дописывается немедленно —
  до этого терялись символы, набранные в последние полсекунды перед закрытием вкладки.

Предупреждение «по проценту от квоты» намеренно не делали: с `unlimitedStorage` предел
в 10 МБ не действует, и такая полоса врала бы. Вместо неё — честный размер и совет
сделать копию.

### 1.2. Две вкладки одной страницы затирают друг друга — **сделано**

Было: `saveAll()` перезаписывала оба ключа целиком, а чужие изменения игнорировались,
если на странице открыта правка. Побеждала вкладка, сохранившая последней.

Сделано: перед записью читается текущее содержимое ключей, списки сводятся по `id`
(побеждает больший `updatedAt`, при равенстве — своя версия), заметки в правке
и перетаскивании неприкосновенны. Чужое изменение перерисовывает только затронутые
заметки (`syncDom()`), а не всю страницу. Удаление оставляет надгробие в `nis:tombstones`
на 30 дней — иначе слияние воскрешало бы удалённое из чужой памяти; импорт снимает
надгробия с восстанавливаемых `id`.

### 1.3. Флаг `selfWrite` — гонка по таймеру — **сделано**

Было: флаг снимался через 80 мс. Чужая запись, попавшая в это окно, терялась;
затянувшаяся своя вызывала лишнее перечитывание.

Сделано: сравнивается само записанное значение — таймеров нет. Пометка ставится **до**
`await`: уведомление об изменении приходит раньше, чем разрешается промис записи (поймано
на проверке — вкладка перечитывала хранилище после каждого своего сохранения).

Заодно `test/sandbox.html` научился настоящему `onChanged`: хранилище общее для вкладок
песочницы, события приходят и писателю, и соседям. Две открытые вкладки песочницы теперь
ведут себя как две вкладки одной страницы с установленным расширением — на них это
и проверялось.

### 1.4. Отмена удаления — **сделано**

Было: ✕ спрашивал подтверждение, дальше удаление окончательное. Диалог раздражал
на каждом удалении и от промаха не спасал.

Сделано: диалог убран, заметка удаляется сразу, внизу экрана на 8 секунд — «Заметка
удалена · Вернуть». Дальше три дня в `nis:trash`, откуда её возвращает раздел «Недавно
удалённые» в окне расширения (туда же попадает и то, что стёрла кнопка «Удалить все
заметки этой страницы»). Пустая заметка удаляется молча — возвращать нечего.

Возврат проставляет заметке свежее `updatedAt`: иначе её собственное надгробие,
прочитанное при следующем слиянии, спрятало бы её снова. Заодно удалён `askConfirm()` —
после этой правки он больше нигде не вызывается.

### 1.5. Проверка импортируемого файла — **сделано**

Было: импорт сливал в хранилище всё, что подошло под маску ключа, не глядя на содержимое.

Уточнение к первоначальной оценке: разметку и раньше чистил `NIS_SANITIZE` при отрисовке,
так что скрипт из файла на страницу не попадал. Настоящей дырой было поле `extra` —
оно уходило прямо в `el.style.setProperty()`, то есть чужой файл мог задать заметке любое
CSS-свойство, включая фоновую картинку с постороннего адреса (отметка о просмотре).
И типы полей не проверялись вовсе: `x: "сто"` давало `NaN`.

Сделано: `cleanNote()` чинит то, что чинится (числа приводятся и зажимаются, цвета только
`#rrggbb`, шрифт только из списка), и выбрасывает то, что нет (нет внятного `id`, `html`
не строка или больше 64 КБ). Селекторы `anchor`/`flow` проверяются на разбираемость,
`extra` сведён к трём свойствам панели, ключи — к разбираемому `origin`, настройки из файла
приводятся к схеме. `html` чистится на входе, а не только при отрисовке. По итогам —
отчёт: добавлено / уже были / не похожи на заметки.

### 1.6. Напоминание о резервной копии — **S**

Если экспорта не было больше месяца — ненавязчивая строка в окне расширения. До появления
синхронизации (этап 5) это единственная защита от переустановки браузера.

---

## Этап 2. Производительность и вежливость к чужим страницам

### 2.1. Спящий режим на страницах без заметок — **M**

Сейчас на каждой открытой странице создаётся Shadow-слой, вешается `MutationObserver`
на весь `documentElement` вместе с атрибутами и работает вечный `setInterval` раз в 800 мс
(`src/content.js:1703`). На тяжёлых приложениях — почта, таблицы, карты — постоянная
нагрузка без всякой пользы, потому что заметок там нет.

Если для страницы заметок нет, не должно создаваться ничего: ни слоя, ни наблюдателя,
ни интервала — до первого запроса на создание заметки. Самая заметная оптимизация
и лучший ответ на вопрос «а оно не тормозит?».

### 2.2. Счётчики без полного чтения хранилища — **S**

`updateTotals()` делает `chrome.storage.local.get(null)` — вытягивает всё хранилище ради
двух чисел. При сотнях страниц окно расширения начнёт открываться с задержкой. Держать
счётчики в отдельном ключе-индексе.

### 2.3. Наблюдатель за DOM поточнее — **S**

Подписка на `attributes` по всему поддереву — самая дорогая часть. Сузить до реально
нужного или перевести проверку перекрытия на `IntersectionObserver`.

---

## Этап 3. Чего не хватает по функциям

### 3.1. Глобальный список заметок с поиском — **L**

Главный функциональный пробел. Окно расширения показывает только заметки текущей страницы;
заметку, оставленную месяц назад, найти нельзя никак — нужно вспомнить страницу и открыть её.

Отдельная страница (`options_page` или `all.html`): все заметки, поиск по тексту,
группировка по сайтам, сортировка по дате, переход к заметке на странице.

### 3.2. Фильтр по цвету — **S**

Цвет заметки уже задаётся, но ни на что не влияет. Фильтрация по цвету в глобальном списке
превращает его в систему меток без новой сущности в интерфейсе. **Зависит от 3.1.**

### 3.3. Текстовый якорь вторым слоем — **L**

`cssPath()` строит точный, но хрупкий путь (`#article > p:nth-of-type(3)`): сайт вставил
блок выше — путь мимо, заметка падает на абсолютные координаты и уезжает.

Сохранять рядом текстовую цитату (30–40 символов до, точный фрагмент, столько же после) —
подход W3C Web Annotation, на котором работает Hypothesis. При провале CSS-пути искать
текст по странице. Переживает перевёрстку, переезд блока и частично редизайн сайта.

---

## Этап 4. Инженерная база

Отдельный этап, потому что без него каждая следующая правка дороже предыдущей. Технически
может идти параллельно с этапами 1–3.

| Пункт | Объём | Суть |
| --- | --- | --- |
| Общий `src/common.js` | S | `DEFAULTS`, `FONTS`, `SIZES`, `normPath`, ключи хранилища продублированы в `content.js` и `popup.js` — разъедутся |
| Тесты на чистых функциях | M | `node:test` без браузера: `sanitize`, `normPath`, `fileSlug`, слияние при импорте. Полсотни строк закрывают самое рискованное место — санитайзер |
| Скрипт сборки `build.mjs` | M | Версия сейчас вручную синхронизируется между `manifest.json`, README, `site/changelog.html` и именем ZIP. Один скрипт: поднять версию, проверить совпадение, собрать архив без `test/`, `.claude/`, `site/`, `store/` |
| Разбить `content.js` (1806 строк) | M | Границы уже есть по смыслу: якоря, встраивание в поток, редактор, перекрытие, хранилище. Это перемещение кода, а не переписывание |
| Санитайзер: квадратичный обход | S | При запрещённом теге `walk()` перезапускается с начала узла (`src/sanitize.js:44`) — тяжело на большой вставке из Word. Плюс нет общего ограничения на размер HTML заметки |

---

## Этап 5. Синхронизация между устройствами — **L**

Данные только локальные: при переустановке Chrome всё держится на том, вспомнил ли
пользователь сделать экспорт. Полноценный `chrome.storage.sync` не подходит — 100 КБ всего
и 8 КБ на ключ.

Два шага:

1. Настройки и список отключённых сайтов — в `sync` сразу, это в лимит укладывается. **S**
2. Заметки — опциональная синхронизация с разбиением по страницам и разрешением конфликтов
   по `updatedAt`. **L**, и только после этапа 1: механика слияния оттуда здесь
   переиспользуется.

---

## Этап 6. Сайт на HTTPS — **M**

Сайт работает только по http. Это не косметика: Chrome помечает адрес «Не защищено»,
а ZIP, скачанный по http, браузер относит к небезопасным загрузкам и может заблокировать —
то есть основной сценарий установки ломается у части посетителей.

На ho.ua сертификат не выписать: раздела SSL в панели нет, заявка в поддержку отклонена
автоответом (аккаунт на бесплатном тарифе, проверено 17.09.2026).

Перенести статику на Cloudflare Pages или GitHub Pages: бесплатно, HTTPS из коробки, деплой
по push вместо ручной заливки по FTP. ho.ua оставить редиректом. После переноса — поправить
`canonical`, OG-теги и `sitemap.xml`, которые сейчас намеренно указывают http.

Может идти параллельно всему остальному — кода расширения не касается.

---

## Этап 7. Публикация в Chrome Web Store

### 7.1. Сузить права — **L**

`host_permissions: <all_urls>` плюс content script на всех страницах — это и красная строка
при проверке («читать и изменять все ваши данные на всех сайтах»), и медленное ревью.
Альтернатива — `optional_host_permissions` с выдачей доступа по сайту: пользователь
разрешает конкретный домен при первой заметке. Работы прилично, но это снимает главный
барьер доверия — и в описании магазина, и в будущих отзывах.

### 7.2. Локализация — **M**

Все строки зашиты по-русски прямо в коде. `_locales` + `chrome.i18n` дают украинскую
и английскую версии; без этого карточка находится в поиске магазина только по русским
запросам.

### 7.3. Доступность — **M**

В `src/popup.html` и `src/styles.js` нет ни одного `aria-label` и `role`. Кнопки панели —
голые эмодзи (📌 🌐 – ✕), скринридер прочитает их как символы. Создать и переместить
заметку только с клавиатуры сейчас нельзя.

### 7.4. Мелочи карточки — **S** каждая

* Тёмная тема: `prefers-color-scheme` не используется нигде.
* Печать страницы: поведение заметок в `@media print` не описано — решить, печатаются они
  или прячутся.
* Заметки не работают внутри iframe (`all_frames: false`). Если это осознанно — написать
  в FAQ, иначе будут отзывы «не работает на сайте N».
* Контрольная сумма ZIP на странице загрузки.
