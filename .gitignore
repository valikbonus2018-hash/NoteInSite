# Реквизиты доступа к хостингу — в историю не попадают никогда.
# Правильнее держать этот файл вообще вне папки проекта.
data.txt

# Сборки расширения: пересобираются из исходников, версии помечаются тегами.
# ZIP в site/download/ — часть выкладываемого сайта, он в истории остаётся.
dist/

# Локальные разрешения Claude Code — у каждой машины свои
.claude/settings.local.json

# Мусор операционных систем и редакторов
.DS_Store
Thumbs.db
desktop.ini
*.log
*.swp
*~
